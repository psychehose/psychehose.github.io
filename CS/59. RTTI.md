
RTTI (Run-Time Type Information)는 C++에서 프로그램 실행 중에 객체의 데이터 타입을 식별할 수 있게 해주는 메커니즘

보통 C++은 컴파일 시간에 타입을 결정하는 정적 타입 언어이지만, 다형성(Polymorphism)을 사용할 때 부모 클래스 포인터가 실제로 어떤 자식 클래스의 인스턴스를 가리키고 있는지 알아야 할 때가 있는데, 이 때 RTTI를 이용함

핵심적인 기능 두 가지는 `dynamic_cast`와 `typeid`